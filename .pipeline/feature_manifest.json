{
  "_behavioral_specs": {
    "_global": [
      {
        "text": "## Summary\nBuild a FastAPI + SQLite backend with JWT auth and per-user book CRUD, plus a React (Vite + TypeScript) frontend with login/signup, a URL-addressable shelf with status filtering, and add/rate flows — deployed as a single container.\n\n## Scope\nGreenfield: all files are new. Layout: `backend/` (FastAPI) and `frontend/` (React/Vite), with a root `Dockerfile` serving the built SPA from FastAPI.\n\n### Files to create — Backend (`backend/`)\n- `app/main.py` — FastAPI app, CORS, router mounts, static-file serving of built frontend, `/api/health` + `/api/health/deep` endpoints.\n- `app/database.py` — SQLModel engine + session dependency (SQLite file `booknook.db`); create tables on startup.\n- `app/models.py` — `User` (id, email unique, hashed_password, is_admin, created_at) and `Book` (id, owner_id FK, title, author, status enum, rating nullable 1–5, created_at).\n- `app/schemas.py` — Pydantic: `UserCreate`, `UserRead`, `Token`, `BookCreate`, `BookUpdate` (status + optional rating), `BookRead`. Status enum: `WANT_TO_READ | READING | FINISHED`.\n- `app/auth.py` — password hashing (passlib bcrypt), JWT encode/decode (python-jose), `get_current_user` dependency, secret from env `JWT_SECRET`.\n- `app/routers/auth_router.py` — `POST /api/auth/register` (first user → is_admin=True), `POST /api/auth/login` (401 on bad creds, returns token).\n- `app/routers/books_router.py` — `GET /api/books?status=`, `POST /api/books` (defaults WANT_TO_READ), `PATCH /api/books/{id}` (status + rating, ownership-checked), `DELETE /api/books/{id}`. All require auth and scope to `current_user`.\n- `requirements.txt` — fastapi, uvicorn, sqlmodel, python-jose[cryptography], passlib[bcrypt], python-multipart, pydantic[email].\n- `tests/test_auth.py`, `tests/test_books.py` — pytest + TestClient covering each spec scenario.\n\n### Files to create — Frontend (`frontend/`)\n- `index.html`, `vite.config.ts`, `tsconfig.json`, `package.json` — Vite React-TS scaffold; dev proxy `/api` → backend.\n- `src/main.tsx`, `src/App.tsx` — React Router setup.\n- `src/api/client.ts` — fetch wrapper injecting `Authorization: Bearer <token>`; 401 → redirect `/login`.\n- `src/auth/AuthContext.tsx` — token in localStorage, `login`/`register`/`logout`, current-user state.\n- `src/components/RequireAuth.tsx` — route guard redirecting to `/login`.\n- `src/pages/LoginPage.tsx`, `src/pages/SignupPage.tsx` — email/password forms.\n- `src/pages/ShelfPage.tsx` — book list, status filter bound to `?status=`, add/rate modals via `?modal=`.\n- `src/components/BookCard.tsx` — title, author, status control, star rating display; delete.\n- `src/components/AddBookModal.tsx`, `src/components/RateBookModal.tsx`.\n- `src/types.ts` — shared TS types mirroring backend schemas.\n\n### Files to create — Deploy\n- `Dockerfile` — multi-stage: build frontend (`npm run build`), copy `frontend/dist` into image, install backend deps, run uvicorn serving API + static SPA.\n- `.dockerignore`, `backend/.env.example` (`JWT_SECRET`).\n\n### Dependencies\nBackend: fastapi, uvicorn, sqlmodel, python-jose[cryptography], passlib[bcrypt], python-multipart, pydantic[email]. Frontend: react, react-dom, react-router-dom, vite, typescript.\n\n### Assumptions\n- Stack is FastAPI + React (from repo name `e2e-fastapi-react-booknook`); greenfield, so conventions established here.\n- Auth model is `full_auth`: login + signup + logout + guards on all app routes; first registered user becomes admin. Public routes: `/login`, `/signup`, `/api/health`, `/api/health/deep`.\n- JWT bearer tokens (stateless) — the Angular/React/FastAPI-appropriate pattern.\n- SQLite for persistence (single-container simplicity); SQLModel as ORM.\n- Books are strictly per-user; no sharing.\n- Rating allowed only when status is FINISHED; validated 1–5.\n\n## Implementation Plan\n\n### Step 1: Backend scaffold + DB\n**Files:** `backend/app/database.py`, `models.py`, `requirements.txt`\n**Change:** Define SQLModel engine/session dependency and `User`/`Book` models with the status enum. Create tables on startup.\n\n### Step 2: Auth\n**Files:** `backend/app/auth.py`, `routers/auth_router.py`, `schemas.py`\n**Change:** Implement bcrypt hashing, JWT issue/verify, `get_current_user`. `register` (first user admin), `login` returning token; wrong password → 401.\n\n### Step 3: Books API\n**File:** `backend/app/routers/books_router.py`\n**Change:** CRUD scoped to `current_user`. `GET` accepts optional `status` filter. `POST` defaults WANT_TO_READ. `PATCH` updates status/rating with ownership + rating-range checks (404 on foreign/missing book).\n\n### Step 4: App wiring + health\n**File:** `backend/app/main.py`\n**Change:** Mount routers under `/api`, add CORS, `/api/health` (liveness) and `/api/health/deep` (DB check), and static SPA fallback for non-API routes.\n\n### Step 5: Frontend scaffold + auth\n**Files:** Vite scaffold, `AuthContext.tsx`, `client.ts`, `RequireAuth.tsx`, `LoginPage.tsx`, `SignupPage.tsx`\n**Change:** Router with public `/login`, `/signup` and guarded app routes. Successful sign-in stores token and navigates to `/shelf`.\n\n### Step 6: Shelf + filtering + modals\n**Files:** `ShelfPage.tsx`, `BookCard.tsx`, `AddBookModal.tsx`, `RateBookModal.tsx`\n**Change:** Load books; filter tabs write `?status=` and restore from URL on load. Add flow via `?modal=add-book`; finish/rate via `?modal=rate&bookId=`. Status changes + delete call the API and refresh.\n\n### Step 7: Dockerfile + deploy\n**File:** `Dockerfile`\n**Change:** Multi-stage build; serve built SPA from FastAPI; expose uvicorn.\n\n### Step 8: Tests\n**Files:** `backend/tests/*`\n**Change:** Cover sign-in success/401, add (defaults WANT_TO_READ), status update, finish+rate, and status filter.\n\n## Testing Strategy\n- Backend: pytest + TestClient for each spec scenario (sign-in ok/401, add→WANT_TO_READ, update→READING, finish+rate→FINISHED w/ rating, `?status=` filter returns only matching).\n- Auth: token required on `/api/books/*`; unauthenticated → 401.\n- Frontend: manual/E2E — register, sign in, add book, change status, rate, filter; verify `?status=` deep-links restore filter and modals open from URL params.\n- Health: `curl /api/health` and `/api/health/deep`.\n\n## Risks\n- bcrypt/passlib version pinning can break builds — pin known-good versions.\n- SQLite is single-writer; fine for this scale but note if scaling later.\n- Ensure JWT_SECRET is set in the deploy env; app should fail fast if missing in production.",
        "type": "architectural",
        "source": "plan",
        "rev": 0,
        "superseded": false
      }
    ]
  },
  "_component_requirements": {
    "_global": {
      "requirements": [
        {
          "text": "Revision: on the sign-in page make the primary button teal, and add a \"Remember me\" checkbox above it.",
          "type": "behavioral",
          "source": "feedback",
          "rev": 1
        }
      ]
    }
  }
}